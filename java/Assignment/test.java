package Assignment;

public class test {

	public static void main(String[] args) {
		int count r;
			int indx = 0;
			
			while (indx < count) {
				if (x < data[indx]) {
					while(indx < count) { //count 사용
						int temp = data[indx];
						data[indx] = x;
						x = data[++indx];
					}
					break;
				}
				else {
					indx ++;
				}
			}
			//count ++ ;
			return ++count;
		}
	}

}
